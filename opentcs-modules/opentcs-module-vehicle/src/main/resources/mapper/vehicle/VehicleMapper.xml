<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.opentcs.vehicle.mapper.VehicleMapper">

    <select id="selectPageVehicle" resultType="org.opentcs.vehicle.domain.entity.Vehicle">
        SELECT * FROM vehicle
        <where>
            <if test="param2.name != null and param2.name != ''">AND name LIKE CONCAT('%', #{param2.name}, '%')</if>
            <if test="param2.state != null and param2.state != ''">AND state = #{param2.state}</if>
            <if test="param2.vinCode != null and param2.vinCode != ''">AND vin_code LIKE CONCAT('%', #{param2.vinCode}, '%')</if>
            <if test="param2.vehicleTypeId != null">AND vehicle_type_id = #{param2.vehicleTypeId}</if>
        </where>
    </select>

</mapper>