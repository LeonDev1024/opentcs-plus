<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.opentcs.kernel.persistence.mapper.NavigationMapMapper">

    <select id="selectPageNavigationMap" resultType="org.opentcs.kernel.persistence.entity.NavigationMapEntity">
        SELECT * FROM navigation_map
        WHERE del_flag = '0'
        <if test="navigationMap.factoryModelId != null">
            AND factory_model_id = #{navigationMap.factoryModelId}
        </if>
        <if test="navigationMap.name != null and navigationMap.name != ''">
            AND name LIKE CONCAT('%', #{navigationMap.name}, '%')
        </if>
    </select>

    <select id="selectByFactoryModelId" resultType="org.opentcs.kernel.persistence.entity.NavigationMapEntity">
        SELECT * FROM navigation_map
        WHERE factory_model_id = #{factoryModelId}
          AND del_flag = '0'
        ORDER BY floor_number ASC, name ASC
    </select>

    <select id="selectByFactoryModelIdAndFloor" resultType="org.opentcs.kernel.persistence.entity.NavigationMapEntity">
        SELECT * FROM navigation_map
        WHERE factory_model_id = #{factoryModelId}
          AND floor_number = #{floorNumber}
          AND del_flag = '0'
        LIMIT 1
    </select>

</mapper>
